#include "main.h"
/**
 * puts2 - prints string and adds newline
 * @s: string to be printed
 */
void puts2(char *str)
{
	int counter;
	char *tmp;
	int i;

	counter = 0;
	while (*s != '\0')
	{
		if (counter %  == 0)
			_putchar(*s);
		counter++;
		s++;
	}
	_putchar('\n');
}
