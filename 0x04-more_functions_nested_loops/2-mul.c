#include "main.h"
/**
 * mul - multiplies two integers
 *@a: first multipl
 *@b: second multiple
 * Return: returns the product of the multiplication
 */
int mul(int a, int b)
{
	long int product;

	product = (long int) a * b;
	return (product);
}
